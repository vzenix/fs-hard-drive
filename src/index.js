
const exec = require('child_process').exec,
const os = require('os');
const fs = require('fs');

export.Utils = Utils;
export.Disk = Disk;

function Utils() {}

function Disk() {}

console.log("TEST CASE");