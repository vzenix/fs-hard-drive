const assert = require('assert');
const {
    lsDevices
} = require('../../../src/index');

describe('lsDevince', () => {
    it('Test 01:', () => {
        // lsDevices((res) => console.log('Response: ', res));
        assert.equal(1, 1);
    });
});